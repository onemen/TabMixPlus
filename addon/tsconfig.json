{
  "extends": "../config/base.tsconfig.json",
  "include": ["*.js", "../@types/index.d.ts", "../@types/general.d.ts"],
  "exclude": [
    "node_modules",
    "**/gecko/**/*.d.ts",
    "logs/**",
    "**/*~/*",
    "**/*~*.*",
    "**/*.local.*",
    // files with different scopes
    "chrome/**",
    "defaults/**",
    "modules/**"
  ],
  "compilerOptions": {
    "types": ["addon"]
  }
}
