{
  "extends": "../config/base.tsconfig.json",
  "include": ["*.js"],
  "exclude": [
    "node_modules",
    "**/gecko/**/*.d.ts",
    "logs/**",
    "**/*~/*",
    "**/*~*.*",
    "**/*.local.*",
    // files with different scopes
    "chrome/**",
    "defaults/**",
    "modules/**"
  ],
  "compilerOptions": {
    "types": ["../types/general.d.ts", "../types/tabmix.d.ts"]
  }
}
