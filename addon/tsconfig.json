{
  "extends": "../config/base.tsconfig.json",
  "include": ["*.js"],
  "exclude": [
    "node_modules",
    "**/gecko/**/*.d.ts",
    "logs/**",
    "**/*~/*",
    "**/*~*.*",
    "**/*.local.*",
    // files with different scopes
    "chrome/**",
    "defaults/**",
    "modules/**"
  ],
  "compilerOptions": {
    "types": ["index.d.ts", "general.d.ts"]
  }
}
